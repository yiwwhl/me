{
  "builds": [
    {
      "src": "packages/web/package.json",
      "use": "@vercel/next",
      "config": {
        "installCommand": "pnpm install --prefer-offline",
        "buildCommand": "pnpm run build"
      }
    }
  ]
}